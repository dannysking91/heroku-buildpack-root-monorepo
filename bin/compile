#!/bin/sh

BUILD_DIR=$1

STAGE="$(mktemp -d)"

mv "${BUILD_DIR}"/* "${STAGE}"
rm -rf "${BUILD_DIR}"/*
mkdir "$BUILD_DIR/web/"
mv "${STAGE}" "${BUILD_DIR}/web/"
mv "/tmp/js-packages" "${BUILD_DIR}/js-packages/"
ls -la
cd web
ls -la
cd modules
ls -la
cd inc.joinroot.com
ls -la