#!/bin/sh

BUILD_DIR=$1

STAGE="$(mktemp -d)"

mv "${BUILD_DIR}/" "${STAGE}"
rm -rf "${BUILD_DIR}"/*
mkdir "$BUILD_DIR/web/"
mv "${STAGE}" "${BUILD_DIR}/web/"
ls -la "$BUILD_DIR/"
ls -la "$BUILD_DIR/web/"